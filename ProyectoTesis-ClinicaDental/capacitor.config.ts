import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProyectoTesis-ClinicaDental',
  webDir: 'www'
};

export default config;
